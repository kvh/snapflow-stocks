name: Ticker
version: 1
description: Stock ticker and metadata
unique_on:
  - symbol
  - exchange
immutable: false
fields:
  symbol:
    type: Unicode(255)
    validators:
      - NotNull
  exchange:
    type: Unicode(255)
    validators:
      - NotNull
  name:
    type: Unicode(255)
