name: Ticker
version: 1
description: Stock ticker and metadata
unique on:
  - symbol
  - exchange
on conflict: ReplaceWithNewer
fields:
  symbol:
    type: Unicode(255)
    validators:
      - NotNull
  exchange:
    type: Unicode(255)
    validators:
      - NotNull
  name:
    type: Unicode(255)
